var Template = module.exports = require('./main')
  , for_handler = require('./tags/for')
  , if_handler = require('./tags/if')
  , value = require('./tags/value')
  , add = require('./filters/add')
  , html = require('./tags/html')
  , attr = require('./tags/attr')

Template.add_tag('altr-attr', attr)
Template.add_tag('altr-html', html)
Template.add_tag('altr-value', value)
Template.add_tag('altr-if', if_handler)
Template.add_tag('altr-for', for_handler)

Template.add_filter('add', add)
